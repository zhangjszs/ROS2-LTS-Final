# AGENTS.md

Guidance for any AI coding agent working in this repository.

The full project context — build/launch commands, local validation loop, architecture, CMake and C++20 conventions — lives in **[CLAUDE.md](CLAUDE.md)**. Read and follow it; start with its "Local Validation Loop" section before editing, and run the matching `scripts/*.sh` gate after your changes.

This file is intentionally a pointer, not a copy, so the two never drift.
